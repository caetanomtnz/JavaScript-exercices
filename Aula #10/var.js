  /* variável clima, recebendo uma string(Quente) */
  var clima="quente"

  /* Pra mudar é so coloca outro texto dentro da string */
  var clima="frio"
  
  /* Pra ver, console.log(clima) */
  console.log(clima)